div.title-error
    h1 #{errorNames.text.h1.error}
        h2 #{errorNames.text.h1.title}
            p.text-error #{errorNames.text.h1.firstLine}
if isAuthenticated
    form(action="/menu", method="GET") 
        button.btn.index-button.button-error(type="submit") #{errorNames.form.menu}
else
    form(action="/", method="GET") 
        button.btn.index-button.button-error(type="submit") #{errorNames.form.index}